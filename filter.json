{
  "name": "world_template_exporter",
  "version": "1.0.1",
  "runWith": "python",
  "script": "main.py",
  "settings_schema": {
    "type": "object",
    "properties": {
      "target_path": {
        "type": "string",
        "description": "The absolute path to the Minecraft world_template folder."
      },
      "copy_packs": {
        "type": "boolean",
        "description": "Should the filter copy packs to the world template folder?"
      },
      "world_template_path": {
        "type": "string",
        "description": "The path to the world template folder inside the 'data' directory."
      }
    },
    "required": [
      "target_path",
      "copy_packs",
      "world_template_path"
    ]
  }
}